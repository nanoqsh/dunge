[workspace]
members = ["dunge", "dunge_macros", "dunge_shader"]
default-members = ["dunge"]
resolver = "2"

[profile.dev]
opt-level = 1
panic = "abort"
split-debuginfo = "unpacked"

[profile.dev.package."*"]
opt-level = 3

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
