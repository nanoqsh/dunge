[package]
name = "triangle"
version = "0.1.0"
edition = "2021"

[dependencies]
dunge = { path = "../../dunge", features = ["winit"] }
env_logger = "0.10"
helpers = { path = "../../helpers" }

[lints]
workspace = true
