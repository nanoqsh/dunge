[package]
name = "triangle"
version = "0.1.0"
edition = { workspace = true }

[dependencies]
dunge = { workspace = true, features = ["winit"] }
env_logger = "0.10"
helpers = { path = "../../helpers" }

[lints]
workspace = true
