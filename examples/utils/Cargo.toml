[package]
name = "utils"
version = "0.1.0"
edition = "2021"

[dependencies]
dunge = { path = "../../dunge", default-features = false }
image = { version = "0.24", default-features = false, features = ["png"] }
